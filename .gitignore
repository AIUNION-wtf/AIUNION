# NEVER push these to GitHub
config.py
*.env
.env

# Admin override and scorch key tools — manual-only, never run automatically
admin_sign.py
scorch.py

# Encrypted keyfiles — store securely offline, never commit
K_admin.json
K_scorch.json
*.keyfile.json

# Output files generated by admin_sign.py and scorch.py
admin_unsigned_*.psbt
*_admin_signed.psbt
*_admin_signed.hex
scorch_tx_*.hex
scorch_tx_*.psbt

# Admin action log — local only
admin_log.txt

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# Local scratch / backups / transcripts
archive_all.py
*.backup.*.json
*claude conversation*.txt
*conversation*.txt
